---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);
---

<Layout title="Dr. Alper Mumcu | Kadın Hastalıkları ve Doğum Uzmanı">
	<main>
		<!-- Hero Section -->
		<section class="hero">
			<div class="container hero-content">
				<div class="hero-text">
					<span class="badge">Kadın Hastalıkları ve Doğum Uzmanı</span>
					<h1>Modern Yaklaşımla <br/><span class="text-gradient">Kadın Sağlığı Bilgisi</span></h1>
					<p>Dr. Alper Mumcu'nun 25 yılı aşkın tecrübesiyle hazırlanan, güncel ve bilimsel kadın sağlığı platformuna hoş geldiniz.</p>
					<div class="hero-actions">
						<a href="/online-randevu" class="btn btn-primary">Randevu Al</a>
						<a href="/dr-alper-mumcu" class="btn btn-outline">Hakkında</a>
					</div>
				</div>
				<div class="hero-decoration">
					<div class="blob"></div>
				</div>
			</div>
		</section>

		<!-- Services Grid -->
		<section class="services">
			<div class="container">
				<div class="section-header">
					<h2 class="font-outfit">Hizmet Alanları</h2>
					<p>Size özel uzmanlık alanlarımız ve bilgilendirici içeriklerimiz.</p>
				</div>
				<div class="grid">
					<Card 
						href="/hamilelik/" 
						title="Hamilelik" 
						body="Hafta hafta gebelik takibi, beslenme ve merak edilen her şey."
					/>
					<Card 
						href="/dogum/" 
						title="Doğum" 
						body="Doğum yöntemleri, hazırlık süreci ve sonrası için rehber bilgiler."
					/>
					<Card 
						href="/jinekoloji/" 
						title="Jinekoloji" 
						body="Genel kadın sağlığı, cerrahi işlemler ve güncel yaklaşımlar."
					/>
					<Card 
						href="/infertilite/" 
						title="İnfertilite" 
						body="Kısırlık tedavisi, tüp bebek süreçleri ve yeni teknolojiler."
					/>
				</div>
			</div>
		</section>

		<!-- Latest Blog Posts -->
		<section class="blog-preview">
			<div class="container glass blog-container">
				<div class="section-header">
					<h2 class="font-outfit">Son Makaleler</h2>
					<p>Kadın sağlığı ve hamilelik üzerine en güncel paylaşımlar.</p>
				</div>
				<div class="grid">
					{posts.map((post) => (
						<article class="post-card">
							<div class="post-date">{post.data.pubDate.toLocaleDateString('tr-TR', { day: 'numeric', month: 'long', year: 'numeric'})}</div>
							<h3>{post.data.title}</h3>
							<a href={`/blog/${post.slug}/`} class="more-link">Devamını Oku &rarr;</a>
						</article>
					))}
				</div>
				<div class="view-all">
					<a href="/blog/" class="btn btn-outline">Tüm Blog Yazılarını Gör</a>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	/* Hero Section */
	.hero {
		min-height: 85vh;
		display: flex;
		align-items: center;
		padding-top: 6rem;
		background: radial-gradient(circle at 10% 20%, hsl(var(--accent) / 0.05), transparent 50%);
		position: relative;
		overflow: hidden;
	}
	.hero-content {
		display: grid;
		grid-template-columns: 1.2fr 0.8fr;
		gap: 4rem;
		align-items: center;
		position: relative;
		z-index: 2;
	}
	.hero-text h1 {
		font-size: 4.5rem;
		line-height: 1.1;
		margin: 1.5rem 0;
		color: hsl(var(--text-main));
		font-weight: 800;
	}
	.hero-text p {
		font-size: 1.35rem;
		color: hsl(var(--text-muted));
		margin-bottom: 3rem;
		max-width: 550px;
		line-height: 1.6;
	}
	.badge {
		background: hsl(var(--accent) / 0.1);
		color: hsl(var(--accent));
		padding: 0.6rem 1.25rem;
		border-radius: 99px;
		font-size: 0.9rem;
		font-weight: 700;
		letter-spacing: 0.02em;
		display: inline-block;
		border: 1px solid hsl(var(--accent) / 0.2);
	}

	/* Buttons */
	.hero-actions {
		display: flex;
		gap: 1.5rem;
	}
	.btn {
		padding: 1.125rem 2.5rem;
		border-radius: 16px;
		font-weight: 700;
		text-decoration: none;
		transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
		display: inline-block;
		font-size: 1.1rem;
	}
	.btn-primary {
		background: var(--accent-gradient);
		color: white;
		box-shadow: 0 10px 25px -5px hsl(var(--accent) / 0.3);
	}
	.btn-primary:hover {
		transform: translateY(-4px);
		box-shadow: 0 20px 30px -5px hsl(var(--accent) / 0.4);
	}
	.btn-outline {
		border: 1px solid var(--glass-border);
		background: white;
		color: hsl(var(--text-main));
		box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.05);
	}
	.btn-outline:hover {
		background: hsl(var(--bg-main));
		transform: translateY(-4px);
		border-color: hsl(var(--accent) / 0.3);
		color: hsl(var(--accent));
	}

	/* Decoration */
	.hero-decoration {
		position: relative;
		height: 500px;
		display: flex;
		justify-content: center;
		align-items: center;
	}
	.blob {
		position: absolute;
		width: 450px;
		height: 450px;
		background: var(--accent-gradient);
		filter: blur(100px);
		border-radius: 50%;
		opacity: 0.15;
		animation: drift 15s infinite alternate ease-in-out;
	}
	@keyframes drift {
		from { transform: translate(-20px, -20px) scale(1); }
		to { transform: translate(40px, 40px) scale(1.1); }
	}

	/* Sections */
	.section-header {
		text-align: center;
		margin-bottom: 5rem;
	}
	.section-header h2 {
		font-size: 3.5rem;
		margin-bottom: 1.25rem;
		color: hsl(var(--text-main));
		font-weight: 800;
	}
	.section-header p {
		color: hsl(var(--text-muted));
		font-size: 1.25rem;
		max-width: 600px;
		margin: 0 auto;
	}

	.grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2.5rem;
	}

	/* Blog Preview */
	.blog-preview {
		padding-bottom: 10rem;
	}
	.blog-container {
		padding: 5rem;
		border-radius: 40px;
		background: white;
		box-shadow: 0 25px 50px -12px rgb(0 0 0 / 0.05);
	}
	.post-card {
		padding: 2rem;
		border-bottom: 1px solid var(--glass-border);
		transition: all 0.3s ease;
		border-radius: 16px;
	}
	.post-card:hover {
		background: hsl(var(--accent) / 0.03);
		transform: translateX(10px);
	}
	.post-date {
		font-size: 0.85rem;
		color: hsl(var(--accent));
		text-transform: uppercase;
		margin-bottom: 0.75rem;
		font-weight: 700;
		letter-spacing: 0.05em;
	}
	.post-card h3 {
		font-size: 1.4rem;
		margin-bottom: 1.25rem;
		line-height: 1.4;
		color: hsl(var(--text-main));
		font-family: 'Outfit', sans-serif;
	}
	.more-link {
		color: hsl(var(--accent));
		text-decoration: none;
		font-weight: 700;
		font-size: 0.95rem;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}
	.view-all {
		text-align: center;
		margin-top: 4rem;
	}

	@media (max-width: 1024px) {
		.hero-content {
			grid-template-columns: 1fr;
			text-align: center;
		}
		.hero-text h1 {
			font-size: 3.5rem;
		}
		.hero-text p {
			margin: 1.5rem auto 2.5rem;
		}
		.hero-actions {
			justify-content: center;
		}
		.hero-decoration {
			display: none;
		}
		.blog-container {
			padding: 2rem;
		}
	}

	@media (max-width: 640px) {
		.hero-text h1 {
			font-size: 2.75rem;
		}
		.section-header h2 {
			font-size: 2.25rem;
		}
	}
</style>
